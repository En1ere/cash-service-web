import {IconName} from "@/components/UI/Icons/source";

export type SvgIconProps = Readonly<{
    width?: number;
    height?: number;
    color?: string;
    name: IconName;
    className?: string;
}>;
